<!doctype html>
<html lang="{{ model.language }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ model.title }}</title>

    <script>
        window.__PATH = "{{ model.path }}";
    </script>
    {% for url in model.jsFiles %}<script src="{{ url }}"></script>{% endfor %}
    {% for url in model.cssFiles %}<link rel="stylesheet" href="{{ url }}">{% endfor %}
</head>
<body>

{% include template %}

</body>
</html>
